import markdown
import os

markdown_content = """# 城市等時線動畫 (Isochrone Map Animation) 設計邏輯解密

這份文件記錄了我們如何使用 Python 將 `41.321254, 2.074613` 這個基地周圍的交通路網，轉化為賽博龐克風格的「水波紋時間擴散動畫」。

---

## 🗺️ 核心概念：什麼是「等時線 (Isochrone)」？
「等時線」代表從一個特定的起點出發，在「相同的時間內」可以到達的所有地點所連成的範圍。
與傳統的「圓形半徑 (例如：方圓 3 公里)」不同，等時線會考慮真實的街道走向、河流阻擋、高架橋等地形限制。

---

## 🛠️ 開發環境與工具 (Tech Stack)
這個專案使用了以下五個核心的 Python 資料科學魔法套件：
1. **OSMnx:** 負責從 OpenStreetMap (開源世界地圖) 抓取真實的街道向量資料。
2. **NetworkX:** 負責處理複雜的拓樸圖 (Graph)，計算最短路徑與時間。
3. **GeoPandas:** 處理地理空間的投影與轉換 (經緯度 ↔ 實際公尺)。
4. **Matplotlib:** 強大的繪圖工具，將冰冷的數據變成有霓虹質感的視覺影像。
5. **ImageIO:** 將畫出來的數十張靜態圖片，串接成流暢的 GIF 動畫。

---

## ⚙️ 製作步驟大解密 (Step-by-Step Logic)

### Step 1: 抓取地球的血管 (獲取路網資料)
我們告訴程式基地的座標 `(41.321254, 2.074613)`，並且設定一個最大範圍 (例如：開車 20 分鐘的距離)。
程式會透過 `osmnx.graph_from_point()` 向雲端請求這個圓形區域內的所有街道。
*   如果是 `walk` (步行)，它會抓取步道、人行道、公園小徑。
*   如果是 `bike` (單車)，它會抓取自行車道和一般馬路。
*   如果是 `drive` (汽車)，它會抓取高速公路、單行道和主要幹道。

### Step 2: 把距離變成時間 (轉換權重)
真實世界的馬路是有長度的 (公尺)。我們需要設定交通工具的平均速度：
*   **步行速度：** 大約是 4.5 公里/小時 ➔ **換算約 75 公尺/分鐘**
*   **單車速度：** 大約是 15 公里/小時 ➔ **換算約 250 公尺/分鐘**
*   **開車速度：** 大約是 36 公里/小時 (市區) ➔ **換算約 600 公尺/分鐘**

接著，程式會跑遍地圖上幾萬條街道，把每一條道路的「長度」除以「速度」，算出走這條路需要花幾分鐘，並把這個時間儲存在那條路上。

### Step 3: 水波紋的擴散 (Dijkstra 演算法)
這是產生動畫最核心的步驟！我們利用圖論中的經典演算法 (通常是 Dijkstra 演算法的變體，在程式中使用 `networkx.ego_graph` 實作)。
我們設定一個時間計時器 `t` (例如：1分鐘、3分鐘、5分鐘...)。
當 `t = 5` 時，程式會從基地節點出發，沿著有相連的馬路往外探索。只要累計的時間超過 5 分鐘，它就停止往前。
此時，所有在 5 分鐘內被踩過的路口節點，就會被標記起來。

### Step 4: 渲染賽博龐克光影 (Matplotlib 繪圖)
為了呈現科技感，我們把整張地圖的底色設為純黑 (`bgcolor='black'`)。
將剛剛在 Step 3 找出來「可到達的節點」塗上發光顏色 (例如：步行的青色 `#00ffff` 或 開車的粉紅色 `#ff00ff`)。
無法到達的節點則保持隱形。
我們用 `matplotlib` 把這個發光的瞬間拍成一張照片存下來。

### Step 5: 拍成電影 (串接 GIF)
當計時器 `t` 從 1 分鐘一路拍到 30 分鐘，我們手裡就有了 15 張漸進的照片。
最後，交給 `ImageIO` 將這些照片串連起來，每張停留 0.4 秒，就完成了一張有著生命力、不斷擴散的城市交通等時線動畫了！🚀

---
*Generated by your personal AI assistant, Nelly ♓*
"""

with open("Isochrone_Design_Logic.md", "w", encoding="utf-8") as f:
    f.write(markdown_content)

print("Markdown saved.")
